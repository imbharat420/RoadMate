import {View, Text} from 'react-native';
import Map from '@components//Map';
const Finder = () => {
  return (
    <View
      style={{
        flex: 1,
      }}>
      <Map />
    </View>
  );
};

export default Finder;
